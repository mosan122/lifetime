name: lifetime
description: A digital time capsule for life milestones.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  dartz: ^0.10.1
  equatable: ^2.0.5
  get_it: ^8.0.0
  http: ^1.2.0
  flutter_bloc: ^8.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  bloc_test: ^9.1.7
  flutter_lints: ^3.0.0
